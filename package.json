{
  "name": "@example/basics",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && tsc --noEmit && astro build",
    "preview": "astro preview",
    "lint": "eslint ./**/*.{js,ts,tsx,astro} --fix --cache --no-error-on-unmatched-pattern",
    "format": "prettier --write ./**/*.{js,ts,tsx,astro} --cache --no-error-on-unmatched-pattern",
    "lint-staged": "lint-staged",
    "postinstall": "husky install"
  },
  "lint-staged": {
    "src/**/*.{js,ts,tsx,astro}": [
      "eslint --fix --cache",
      "prettier --write --cache"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.31.0",
    "@typescript-eslint/parser": "^5.31.0",
    "astro": "^1.0.0-rc.1",
    "eslint": "^8.20.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-astro": "^0.15.0",
    "husky": "^8.0.0",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1",
    "prettier-plugin-astro": "^0.1.2"
  },
  "volta": {
    "node": "18.7.0"
  }
}