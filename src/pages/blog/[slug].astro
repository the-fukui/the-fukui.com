---
import { PostList } from '@data/getPostList'
import postList from '@data/posts.json'

export const getStaticPaths = async () => {
  const posts = postList as PostList
  return posts.map((post) => ({
    params: { slug: post.id },
  }))
}
---

<div>a</div>
